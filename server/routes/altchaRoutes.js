// ALTCHA routes removed.
// The original ALTCHA challenge/verify endpoints were deprecated and removed.
// This file kept as an empty placeholder to avoid import errors during transitional changes.

import express from 'express';
const router = express.Router();

export default router;
